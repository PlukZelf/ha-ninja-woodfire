# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Virtual environments
.venv/
venv/

# Editors
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Local secrets and captures
AGENT_HANDOVER.md
ENVIRONMENT.md
ninja_secrets.json
ninja_secrets.env
ninja_secrets.token.json
*.secrets.json
mnt/
*.log
*.pcap
*.pcapng
*.btsnoop
*btsnoop*
!tools/parse_btsnoop_att.py
captures/*
!captures/.gitkeep

# Home Assistant local runtime
.storage/
home-assistant.log*

# Proprietary/extracted binaries and personal RE working notes — never commit
# (see CLAUDE.md "Nooit committen"): the .so, pulled APKs, and raw Ghidra
# decompile dumps of Ninja's proprietary code all live under tools/artifacts/.
tools/artifacts/
tools/lib/*.so
REBOOT.md
ninja_idtoken.txt
# AWS device-shadow dumps (contain Device_Key + personal identifiers)
aws_devices_dump.json
aws_device_dump.json

# Local reverse-engineering / dev tooling is not distributed. The whole tools/
# tree stays out of the repo: it holds RE scripts, Frida hooks, anti-tamper
# bypass tooling, and captured device-keyed material. Nothing here ships to
# end users (the integration in custom_components/ is fully self-contained).
tools/
